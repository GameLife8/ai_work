# 👋 欢迎使用 AI 运维助手

> 面向运维场景的私有化 Claude Code · 国产模型驱动

---

## 我能做什么

| 类型 | 示例 |
|---|---|
| 🔍 **查信息** | "WYY-DB09 主机所有硬盘当前情况" |
| 🩺 **排障诊断** | "iiot-haitu_seatable 这个服务为什么起不来" |
| ☸️ **K8s 巡检** | "dmz-cluster01 上 default 命名空间所有 pod 状态" |
| 📥 **告警研判** | 直接粘贴一段 Zabbix / 告警 payload JSON |
| ⚡ **执行操作** | "重启 my-app 这个 deployment" — **会先弹卡片确认** |

## 集群切换

右上角 ⚙️ **设置** 里可以切换"当前 Swarm 集群 / Zabbix 实例"，
所有 skill 调用会按你的选择生效。

## 写操作流程

1. 你说出意图（重启 / 扩缩容 / 回滚）
2. 模型分析后会出"操作提议 + 风险评估"
3. 下方弹出 ✅ 确认 / ❌ 拒绝 卡片
4. 点确认后平台自动执行，模型给出最终总结

写操作全程审计落库，可在管理后台查询。

---

> 💡 复杂问题 AI 会先查 `platform_get_runbooks` 拿剧本，按步骤跨层取证。
