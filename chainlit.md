# 👋 欢迎使用 AI 运维助手

> 面向运维场景的私有化 Claude Code · 国产模型驱动

登录后会自动展示**当前接入的所有集群**和**对应的对话示例**。

如果登录后看不到自动跳转，可点 **左上角 ➕** 新建对话。

---

## 核心能力

- **跨集群统一诊断**：自然语言提问，自动选对集群 + 跑相应 skill 取证
- **多层联动**：容器异常 → 自动追溯到宿主机 / 网络 / 内核层
- **写操作护栏**：所有改变集群状态的动作都需用户确认；高危需 admin 二次审批
- **确认后续跑**：点击确认后，平台把真实执行结果接回同一轮分析，不需要你重新提问
- **轻量续跑**：确认后的第二轮只带当前问题断点、tool 调用链和执行结果，不重发完整历史
- **全程审计**：每次 skill 调用、每次会话内容都落库可追溯

---

## 写操作确认流

当模型需要执行 `host_run_command` 或其它写 skill 时，聊天页会出现带 ✅ / ❌ 的确认卡片：

1. 模型先完成只读取证，遇到写操作返回 `needs_confirmation` 后停止 tool-loop。
2. Chainlit 展示确认卡片，最长等待 30 分钟；超时默认拒绝，不会执行命令。
3. 你点击 ✅ 后，平台执行真实操作并把结果通过 `resume_state` 注回 agent。
4. agent 继续同一条 tool-loop：如果证据足够就给结论，若还需要写操作会再次弹确认。

服务/容器到 `IP:PORT` 的连通性问题有额外保护：一旦只读查询已定位运行节点，平台会要求模型下一步进入 `host_run_command` 确认流，避免一直重复 `swarm_query` 却没有确认按钮。

---

## 登录

复用平台账号体系。账号管理在管理后台 → 用户管理。

> 💡 实际可用的资源清单 + 对话示例 在登录进入对话后会**根据你的接入实时生成**。
> 不要照这页死示例打字，看登录后的欢迎消息。
